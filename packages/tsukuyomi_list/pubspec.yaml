name: tsukuyomi_list
description: List views for Tsukuyomi.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ">=3.10.0-290.4.beta <4.0.0"
  flutter: ">=3.38.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # 代码注解
  meta: 1.17.0
  # 向量数学计算
  vector_math: 2.2.0
  # Sliver 组件扩展
  sliver_tools: 0.2.12
  # 集合操作
  collection: 1.19.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 5.0.0
